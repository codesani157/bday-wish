services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: reveal_admin
      POSTGRES_PASSWORD: reveal_password
      POSTGRES_DB: birthday_reveal
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
